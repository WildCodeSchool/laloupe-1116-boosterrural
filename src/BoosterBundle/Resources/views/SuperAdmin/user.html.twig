{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <section class="col-sm-offset-1 col-sm-10 col-xs-12 backWhite sec1">
            <h1>Supprimer un utilisateur</h1>

        </section>>
        {% for user in users %}
            {% if user.enabled %}
                <div class="row">
                    <section class="col-sm-offset-1 col-sm-10 col-xs-12 backWhite sec1">
                        <div class="row downMarg2">
                            <table>
                                <tr>
                                    <th class=" col-xs-4">Nom et Prénom</th>
                                    <th class="col-xs-4">Nom d'utilisateur</th>
                                    <th class=" col-xs-3">Email </th>
                                </tr>
                                <tr>
                                    <td class="col-xs-4">{{ user.lastname }}</td>
                                    <td class="col-xs-4">{{ user.username }}</td>
                                    <td class="col-xs-3">{{ user.email }}</td>
                                    <td class="col-xs-3">
                                        <div class="col-xs-offset-11 col-xs-1">
                                            <a href="{{ path('booster_deleteUser', {'id': user.id}) }}">
                                                <i class="col-xs-offset-11 fa fa-times fa-3x" aria-hidden="true"></i>
                                            </a>
                                        </div>
                                    </td>
                                </tr>

                            </table>

                        </div>

                    </section>
                </div>
            {% endif %}
        {% endfor %}
    </div>


{% endblock %}
