{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-sm-offset-2 col-sm-8 col-xs-12">
            <div class="row">
                <div class="col-sm-offset-3 col-sm-6">
                    <h1>Modification du besoin</h1>

                    {{ form_start(form) }}
                    {{ form_widget(form) }}
                    <a href="{{ path('needs_edit', { 'id': need.id }) }}"><input type="submit" value="Modifier" class="center-block showLien2"></a>
                    {{ form_end(form) }}

                    <ul>
                        <li>
                            {{ form_start(delete_form) }}
                            <input type="submit" value="Supprimer" class="center-block">
                            {{ form_end(delete_form) }}
                        </li>
                        <li>
                            <a href="{{ path('needs_index') }}" class="btn center-block">Retour à la liste</a>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
